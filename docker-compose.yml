services:
  telegram-bot:
    image: python:alpine
    volumes:
      - .:/app
    working_dir: /app
    env_file:
      - .env
    command: sh -c "apk add --no-cache ffmpeg bash build-base libffi-dev && pip install -r requirements.txt && python src/main.py"
    restart: unless-stopped
